# Navidrome Environment Variables
# Copy to .navidrom.env and fill in values (filename typo is intentional — the symlink points here)

# User/Group IDs for file permissions (run `id` to find yours)
PUID=1000
PGID=1000

