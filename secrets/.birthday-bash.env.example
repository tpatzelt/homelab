# Image tag for ghcr.io/tpatzelt/birthday-bash.
# `latest` tracks the rolling tag the repo's own pipeline publishes on every
# push to main. Every build also gets an immutable `sha-<short>` tag, so a
# rollback is an edit here plus `docker compose up -d` — no compose change.
BIRTHDAY_BASH_IMAGE_TAG=latest
