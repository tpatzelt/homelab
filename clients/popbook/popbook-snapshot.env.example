# Copy to /etc/popbook-snapshot.env on popbook (root-owned, mode 0600).
# Optional: without it the script still runs, it just reports nowhere —
# same pattern as secrets/.vpn-egress.env.
HEALTHCHECKS_URL="https://hc-ping.com/<uuid>"
