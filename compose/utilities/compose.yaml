services:
  # librespeed:
  #   container_name: librespeed
  #   image: ghcr.io/librespeed/speedtest:latest
  #   restart: unless-stopped
  #   environment:
  #     MODE: standalone
  #   volumes:
  #     - /opt/dockerdata/librespeed:/database

  # yt-dlp-webui:
  #   container_name: yt-dlp-webui
  #   image: ghcr.io/marcopiovanello/yt-dlp-web-ui:latest
  #   restart: unless-stopped
  #   volumes:
  #     - /opt/dockerdata/yt-dlp-webui/downloads:/downloads
  #     - /opt/dockerdata/yt-dlp-webui/config:/config

  karakeep:
    container_name: karakeep
    image: ghcr.io/karakeep-app/karakeep:0.33.1
    restart: unless-stopped
    volumes:
      - /opt/dockerdata/karakeep/data:/data
    env_file:
      - .env
    environment:
      MEILI_ADDR: http://karakeep-meilisearch:7700
      BROWSER_WEB_URL: http://karakeep-chrome:9222
      DATA_DIR: /data
    networks:
      - default
      - caddy_network

  karakeep-chrome:
    container_name: karakeep-chrome
    image: gcr.io/zenika-hub/alpine-chrome:124
    restart: unless-stopped
    command:
      - --no-sandbox
      - --disable-gpu
      - --disable-dev-shm-usage
      - --remote-debugging-address=0.0.0.0
      - --remote-debugging-port=9222
      - --hide-scrollbars
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:9222/json/version"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s

  karakeep-meilisearch:
    container_name: karakeep-meilisearch
    image: getmeili/meilisearch:v1.50.0
    restart: unless-stopped
    env_file:
      - .env
    environment:
      MEILI_NO_ANALYTICS: "true"
      MEILI_MASTER_KEY: ${MEILI_MASTER_KEY}
      # Meilisearch refuses to start when data.ms was written by an older
      # version — every Renovate bump would otherwise crash-loop until a
      # manual dump/restore (1.49.0 -> 1.50.0 did, 2026-07-25). This migrates
      # data.ms in place on startup instead. Safe to leave on: the index is
      # derived data karakeep can rebuild, and the alternative is a search
      # outage after each bump.
      MEILI_EXPERIMENTAL_DUMPLESS_UPGRADE: "true"
    volumes:
      - /opt/dockerdata/karakeep/meilisearch:/meili_data
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:7700/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s

  ip-tracker:
    container_name: ip-tracker
    image: ghcr.io/tpatzelt/ip-tracker:latest
    restart: unless-stopped
    env_file:
      - .env
    environment:
      PUID: ${PUID}
      PGID: ${PGID}
      TZ: ${TZ}
      IP_TRACKER_INTERVAL_MINUTES: "720"
    volumes:
      - /opt/dockerdata/ip-tracker/data:/app/data
    healthcheck:
      # Image has no wget/curl, but ships python3
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/', timeout=3)"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      - default
      - caddy_network

networks:
  caddy_network:
    external: true
    name: caddy_network
